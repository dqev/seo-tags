{
  "name": "meta-tags-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for meta-tags SEO library",
  "scripts": {
    "build": "turbo run build && node packages/core/scripts/postbuild.js",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "size": "turbo run size",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "turbo": "^2.0.4",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@9.1.4",
  "engines": {
    "node": ">=18.0.0"
  }
}
