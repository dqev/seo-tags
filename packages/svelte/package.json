{
  "name": "@meta-tags/svelte",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "dependencies": {
    "seo-tags": "workspace:*"
  },
  "scripts": {
    "build": "mkdir -p dist && cp src/MetaTags.svelte dist/ && cp src/index.ts dist/ || echo 'done'",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "svelte": ">=4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "svelte": "^4.2.18",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
